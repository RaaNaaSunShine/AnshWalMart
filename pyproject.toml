[project]
name = "ansh-walmart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
	"psycopg[binary]>=3.2",
]

[project.scripts]
ansh-walmart = "ansh_walmart:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
